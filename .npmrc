# React Native/Expo 오토링킹과 일부 라이브러리가 flat한 node_modules를 가정한다.
# pnpm 기본값(isolated)에서는 EAS 빌드가 모듈을 못 찾는 일이 생겨 hoisted로 고정한다.
node-linker=hoisted

# Expo SDK 조합상 peer 범위가 어긋나는 패키지가 있다. npm의 legacy-peer-deps 자리.
strict-peer-dependencies=false
